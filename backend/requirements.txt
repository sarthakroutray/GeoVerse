# Windows Installation Notes:
# Run these commands before installing requirements:
# pip install --upgrade pip setuptools wheel
# 
# For PyTorch CPU version (recommended for Windows):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# 
# For ChromaDB alternative, consider using:
# pip install chromadb-client  # Client-only version without compilation requirements
# 
# If you need FAISS functionality, install pre-compiled wheels:
# pip install faiss-cpu --find-links https://download.pytorch.org/whl/torch_stable.html
# 
# Or use conda for easier package management:
# conda install -c conda-forge faiss-cpu
# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.6.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# Neo4j
neo4j==5.15.0
py2neo==2021.2.4

# Vector Store & Embeddings
# faiss-cpu==1.8.0  # Disabled - compilation issues on Windows
# Alternative: Use pre-compiled wheels or Weaviate/Pinecone instead
sentence-transformers==2.7.0
huggingface_hub==0.24.0
transformers==4.44.2
# chromadb==0.4.18  # Temporarily disabled - requires C++ build tools
weaviate-client==3.25.3  # Alternative vector database (recommended for Windows)
pinecone-client==2.2.4  # Alternative vector database
numpy==1.26.0  # Required for embeddings

# Web Scraping
scrapy==2.11.0
beautifulsoup4==4.12.2
selenium==4.15.2
playwright==1.40.0
requests==2.31.0

# Document Processing
PyMuPDF==1.23.8
python-docx==1.1.0
pytesseract==0.3.10
Pillow==10.1.0

# NLP & ML
spacy==3.7.2
torch==2.2.1  # Will install CPU version by default on most systems
numpy==1.26.0
pandas==2.1.4

# LLM Integration
google-generativeai==0.3.2
openai>=1.0.0
langchain==0.1.7
langchain-community==0.0.20

# Task Queue
celery==5.3.4
redis==5.0.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Utilities
python-dotenv==1.0.0
loguru==0.7.2
click==8.1.7
tqdm==4.66.1
aiofiles==23.2.1

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
black==23.11.0
flake8==6.1.0
mypy==1.7.1
faiss-cpu==1.7.4
# Production
gunicorn==21.2.0
# NOTE: Do not add unpinned 'faiss-cpu' here for Windows; Linux (Render) installs wheels fine via pinned versions above if enabled.