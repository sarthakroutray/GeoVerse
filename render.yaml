services:
  - type: web
    name: geoverse-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python start_production.py
    pythonVersion: 3.11.8
    autoDeploy: true
